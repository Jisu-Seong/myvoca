import FolderComponent from "./../components/voca/FolderComponent";
import BasicLayout from "../layouts/BasicLayout";

const FolderPage = () => {
  return (
    <BasicLayout>
      <FolderComponent />
    </BasicLayout>
  );
};

export default FolderPage;
